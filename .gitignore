node_modules/
dist/
playwright-report/
test-results/
coverage/
server.log
test-output.log
task-server.pid
*.pid
*.log
.env
.env.local
.DS_Store

# Runtime state
gateway-status.json
task-server.pid
*.pid

# Snapshot/restore artifacts (docs/briefs/snapshot-restore.md §6): runtime
# full-state exports + restore checkpoint files — never committed.
storage/snapshots/

# Legacy archive (contains old backups with stale passwords)
_legacy-archive/
*.backup
*.bak

# Secret files
.env.secrets
.env.blogger
.env*
gateway-status.json
